// 未格式化的AZ代码示例
import std.io;
import std.string;

module test.example;

struct Point {
    x: int, y: int, name: string
}

pub struct Vec3 {
    x: float, y: float, z: float
}

enum Result<T, E> {
    Ok(T), Err(E)
}

pub fn add(a: int, b: int) int {
    return a+b;
}

fn main() int {
    let x = 10+20*30;
    var y = x/2;
    if (x>100)
}
