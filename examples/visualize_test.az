// 测试AST可视化的AZ程序
fn factorial(n: int) int {
    if (n <= 1) {
        return 1;
    }
    return n * factorial(n - 1);
}

fn main() int {
    let x = 5;
    let result = factorial(x);
    println("Factorial of " + x + " is " + result);
    return 0;
}