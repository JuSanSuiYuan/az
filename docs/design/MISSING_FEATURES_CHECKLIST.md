# AZ语言缺失功能清单

**更新日期**: 2025年10月30日

---

## 🔴 极高优先级（阻碍实用）

### 1. 标准库 - std.io

**状态**: ❌ 未实现  
**优先级**: 🔴 极高  
**预计时间**: 1周

**缺失功能**:
```az
// ❌ 不能用
import std.io;

// 文件读写
let content = std.io.read_file("data.txt");
std.io.write_file("output.txt", content);

// 标准输入输出
let line = std.io.read_line();
std.io.print("Hello");
std.io.println("World");

// 格式化输出
std.io.printf("x = %d\n", x);
```

**影响**: 不能进行文件操作，严重限制实用性

---

### 2. 标准库 - std.string

**状态**: ❌ 未实现  
**优先级**: 🔴 极高  
**预计时间**: 1周

**缺失功能**:
```az
// ❌ 不能用
import std.string;

// 字符串操作
let len = std.string.length("hello");
let upper = std.string.to_upper("hello");
let lower = std.string.to_lower("HELLO");
let sub = std.string.substring("hello", 0, 3);

// 字符串连接
let s = std.string.concat("Hello", " World");

// 字符串分割
let parts = std.string.split("a,b,c", ",");

// 字符串比较
let eq = std.string.equals("a", "b");

// 字符串格式化
let formatted = std.string.format("x = {}, y = {}", x, y);
```

**影响**: 不能处理字符串，严重限制实用性

---

### 3. 错误处理 - Result类型

**状态**: ❌ 未实现  
**优先级**: 🔴 极高  
**预计时间**: 1周

**缺失功能**:
```az
// ❌ 不能用
enum Result<T, E> {
    Ok(T),
    Err(E)
}

fn read_file(path: string) Result<string, Error> {
    // 实现
}

fn main() int {
    match read_file("data.txt") {
        case Ok(content):
            println(content);
        case Err(e):
            println("Error: " + e);
    }
    return 0;
}
```

**影响**: 不能优雅地处理错误，程序容易崩溃

---

### 4. 错误处理 - Option类型

**状态**: ❌ 未实现  
**优先级**: 🔴 极高  
**预计时间**: 3天

**缺失功能**:
```az
// ❌ 不能用
enum Option<T> {
    Some(T),
    None
}

fn find(arr: []int, target: int) Option<int> {
    // 实现
}

fn main() int {
    match find([1, 2, 3], 2) {
        case Some(index):
            println("Found at: " + index);
        case None:
            println("Not found");
    }
    return 0;
}
```

**影响**: 不能表示可选值，容易出现空指针错误

---

## 🟠 高优先级（影响开发）

### 5. 标准库 - std.collections

**状态**: ❌ 未实现  
**优先级**: 🟠 高  
**预计时间**: 1周

**缺失功能**:
```az
// ❌ 不能用
import std.collections;

// 动态数组
let vec = Vec<int>.new();
vec.push(1);
vec.push(2);
let item = vec.get(0);

// 哈希表
let map = Map<string, int>.new();
map.insert("key", 42);
let value = map.get("key");

// 集合
let set = Set<int>.new();
set.insert(1);
let has = set.contains(1);
```

**影响**: 不能使用动态数据结构

---

### 6. 标准库 - std.fs

**状态**: ❌ 未实现  
**优先级**: 🟠 高  
**预计时间**: 1周

**缺失功能**:
```az
// ❌ 不能用
import std.fs;

// 文件操作
let exists = std.fs.exists("file.txt");
std.fs.remove("file.txt");
std.fs.copy("src.txt", "dst.txt");
std.fs.rename("old.txt", "new.txt");

// 目录操作
std.fs.create_dir("mydir");
std.fs.remove_dir("mydir");
let files = std.fs.read_dir("mydir");

// 路径操作
let path = std.fs.join("dir", "file.txt");
let ext = std.fs.extension("file.txt");
```

**影响**: 不能进行文件系统操作

---

### 7. 模块系统实现

**状态**: ⚠️ 部分实现  
**优先级**: 🟠 高  
**预计时间**: 2-3周

**缺失功能**:
```az
// ❌ 多文件编译不工作

// file1.az
module math;
pub fn add(a: int, b: int) int {
    return a + b;
}

// file2.az
import math;
fn main() int {
    return math.add(1, 2);
}

// 编译
az build main.az  // ❌ 不能自动找到math模块
```

**影响**: 不能组织大型项目

---

### 8. 结构体字面量

**状态**: ⚠️ 部分实现  
**优先级**: 🟠 高  
**预计时间**: 3天

**缺失功能**:
```az
// ❌ 不能用
struct Point {
    x: int,
    y: int
}

fn main() int {
    let p = Point { x: 10, y: 20 };  // ❌ 解析未实现
    return p.x + p.y;
}
```

**影响**: 不能方便地创建结构体实例

---

## 🟡 中优先级（影响体验）

### 9. 包管理器 - chim

**状态**: ❌ 未实现  
**优先级**: 🟡 中  
**预计时间**: 3-4周

**缺失功能**:
```bash
# ❌ 不能用
chim init myproject
chim add json@1.0.0
chim build
chim test
chim publish
```

**影响**: 不能管理依赖，不能使用第三方库

---

### 10. LSP服务器 - AZ lsp

**状态**: ⚠️ 设计完成  
**优先级**: 🟡 中  
**预计时间**: 4-6周

**缺失功能**:
- ❌ 代码补全
- ❌ 跳转定义
- ❌ 查找引用
- ❌ 重命名
- ❌ 错误诊断
- ❌ 悬停提示

**影响**: IDE支持差，开发体验差

---

### 11. 测试框架

**状态**: ❌ 未实现  
**优先级**: 🟡 中  
**预计时间**: 2-3周

**缺失功能**:
```az
// ❌ 不能用
#[test]
fn test_add() {
    assert_eq(add(1, 2), 3);
}

#[test]
fn test_factorial() {
    assert_eq(factorial(5), 120);
}
```

**影响**: 不能写单元测试，质量难以保证

---

### 12. 标准库 - std.math

**状态**: ❌ 未实现  
**优先级**: 🟡 中  
**预计时间**: 3天

**缺失功能**:
```az
// ❌ 不能用
import std.math;

let s = std.math.sqrt(16.0);
let p = std.math.pow(2.0, 3.0);
let a = std.math.abs(-5);
let m = std.math.max(10, 20);
```

**影响**: 不能进行数学计算

---

## 🟢 低优先级（锦上添花）

### 13. 调试器集成

**状态**: ❌ 未实现  
**优先级**: 🟢 低  
**预计时间**: 4-6周

### 14. 性能分析工具

**状态**: ❌ 未实现  
**优先级**: 🟢 低  
**预计时间**: 2-3周

### 15. 代码格式化工具

**状态**: ❌ 未实现  
**优先级**: 🟢 低  
**预计时间**: 1-2周

---

## 📅 实施计划

### Week 1-2: 标准库基础

- [ ] std.io - 文件I/O
- [ ] std.string - 字符串操作
- [ ] Result/Option类型

### Week 3-4: 标准库扩展

- [ ] std.collections - Vec, Map, Set
- [ ] std.fs - 文件系统
- [ ] std.math - 数学函数

### Week 5-7: 模块系统

- [ ] 多文件编译
- [ ] 符号导出/导入
- [ ] 依赖解析

### Week 8-10: 结构体完善

- [ ] 结构体字面量解析
- [ ] 结构体方法
- [ ] 嵌套结构体

### Week 11-14: 包管理器

- [ ] chim基础功能
- [ ] 依赖管理
- [ ] 包发布

### Week 15-20: LSP服务器

- [ ] AZ lsp基础实现
- [ ] 代码补全
- [ ] 跳转定义
- [ ] 错误诊断

### Week 21: 测试和发布

- [ ] 完整测试
- [ ] 性能优化
- [ ] 文档完善
- [ ] 发布v1.0.0

---

## 📊 总结

### 缺失功能统计

- 🔴 极高优先级: 4项
- 🟠 高优先级: 4项
- 🟡 中优先级: 4项
- 🟢 低优先级: 3项
- **总计**: 15项

### 完成时间估算

- **最小可用** (小型项目): 4周
- **中等可用** (中型项目): 7周
- **完整可用** (团队协作): 13周
- **生产就绪** (生产环境): 21周 (5个月)

### 核心结论

**AZ语言目前**:
- ✅ 核心功能完整
- ✅ 适合学习和原型
- ❌ 不适合生产环境
- ⚠️ 需要5个月达到生产就绪

**最关键的缺失**:
1. 标准库 (std.io, std.string, std.collections)
2. 错误处理 (Result, Option)
3. 模块系统实现 (多文件编译)

---

**建议**: 继续完善标准库和错误处理，这是达到实用的关键！

