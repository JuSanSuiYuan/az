# AZ vs Zig - 详细对比

## 概述

AZ和Zig都是现代系统编程语言，都旨在替代C语言，但采用了不同的设计理念和技术路线。

## 核心理念对比

| 方面 | Zig | AZ | 说明 |
|------|-----|----|----|
| **设计目标** | 更好的C | 现代系统编程 | Zig更保守，AZ更激进 |
| **错误处理** | try/catch风格 | Result类型 | AZ借鉴C3 |
| **内存管理** | 手动+Allocator | 手动+可选GC | AZ更灵活 |
| **编译器** | 自研 | LLVM/MLIR | AZ基于成熟生态 |
| **编译时执行** | comptime | comptime | ✅ 相似 |
| **包管理** | 内置 | chim | 都有完整方案 |

## 详细对比

### 1. 错误处理 ⚖️ 不同理念

#### Zig
```zig
const std = @import("std");

fn divide(a: i32, b: i32) !i32 {
    if (b == 0) return error.DivisionByZero;
    return @divTrunc(a, b);
}

pub fn main() !void {
    const result = try divide(10, 2);
    std.debug.print("Result: {}\n", .{result});
}
```

**特点**:
- 使用 `!` 表示可能出错
- `try` 关键字传播错误
- `catch` 捕获错误
- 错误是值，可以返回

#### AZ
```az
fn divide(a: int, b: int) Result<int> {
    if (b == 0) {
        return Result.Err(make_error(
            ErrorKind.RuntimeError,
            "除数不能为零",
            0, 0, ""
        ));
    }
    return Result.Ok(a / b);
}

fn main() int {
    let result = divide(10, 2);
    if (result is Ok) {
        println(result.value);
    } else {
        report_error(result.error);
    }
    return 0;
}
```

**特点**:
- 使用 `Result<T>` 类型
- 显式检查 `is Ok` / `is Err`
- 更接近Rust的风格
- 借鉴C3的设计

**对比**:
- Zig更简洁（`try`关键字）
- AZ更明确（显式Result类型）
- Zig的错误处理更像异常
- AZ的错误处理更像值

### 2. 内存管理 ⚖️ 各有特色

#### Zig
```zig
const std = @import("std");

pub fn main() !void {
    var gpa = std.heap.GeneralPurposeAllocator(.{}){};
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();
    
    // 分配内存
    const buffer = try allocator.alloc(u8, 100);
    defer allocator.free(buffer);
    
    // 使用buffer
}
```

**特点**:
- Allocator模式
- 显式传递allocator
- defer语句自动清理
- 无隐藏分配

#### AZ
```az
fn main() int {
    // 手动内存管理
    let ptr = malloc(100);
    defer free(ptr);
    
    // 或使用可选的GC
    #[gc]
    let obj = new Object();
    // 自动回收
    
    return 0;
}
```

**特点**:
- 默认手动管理
- 可选的AZGC垃圾回收器
- defer语句（类似Zig）
- 计划支持所有权系统

**对比**:
- Zig强制显式allocator
- AZ提供多种选择
- Zig更严格
- AZ更灵活

### 3. 编译时执行 ✅ 都支持

#### Zig
```zig
const std = @import("std");

fn fibonacci(n: u32) u32 {
    if (n <= 1) return n;
    return fibonacci(n - 1) + fibonacci(n - 2);
}

pub fn main() void {
    // 编译时计算
    const fib10 = comptime fibonacci(10);
    std.debug.print("Fibonacci(10) = {}\n", .{fib10});
}
```

#### AZ
```az
fn fibonacci(n: int) int {
    if (n <= 1) return n;
    return fibonacci(n - 1) + fibonacci(n - 2);
}

fn main() int {
    // 编译时计算
    comptime {
        let fib10 = fibonacci(10);
        println("Fibonacci(10) = " + fib10);
    }
    return 0;
}
```

**对比**:
- 两者都支持编译时执行
- Zig的comptime更强大
- Zig可以在任何地方使用comptime
- AZ的comptime更明确

### 4. 类型系统 ⚖️ Zig更强大

#### Zig
```zig
// 泛型
fn max(comptime T: type, a: T, b: T) T {
    return if (a > b) a else b;
}

// 可选类型
const maybe_value: ?i32 = null;

// 错误联合类型
const result: !i32 = error.Failed;

// 联合体
const Value = union(enum) {
    int: i32,
    float: f64,
    string: []const u8,
};

// 向量类型
const vec: @Vector(4, f32) = .{ 1, 2, 3, 4 };
```

#### AZ
```az
// 泛型（计划中）
fn max<T>(a: T, b: T) T {
    return if (a > b) a else b;
}

// Option类型
let maybe_value: Option<int> = Option.None;

// Result类型
let result: Result<int> = Result.Err(error);

// 枚举
enum Value {
    Int(int),
    Float(float),
    String(string)
}

// 向量类型（计划中）
let vec: Vec4<float> = Vec4 { x: 1, y: 2, z: 3, w: 4 };
```

**对比**:
- Zig的类型系统更成熟
- Zig的泛型基于comptime
- AZ的泛型更传统
- Zig支持向量类型（SIMD）

### 5. 模块系统 ⚖️ 不同风格

#### Zig
```zig
// math.zig
pub fn add(a: i32, b: i32) i32 {
    return a + b;
}

// main.zig
const math = @import("math.zig");

pub fn main() void {
    const result = math.add(1, 2);
}
```

**特点**:
- `@import` 导入
- 文件即模块
- 简单直接

#### AZ
```az
// math.az
module math;

pub fn add(a: int, b: int) int {
    return a + b;
}

// main.az
import math;

fn main() int {
    let result = math.add(1, 2);
    return 0;
}
```

**特点**:
- `module` 声明
- `import` 导入
- 类似C3风格

**对比**:
- Zig更简洁
- AZ更明确
- 两者都是文件即模块

### 6. 编译器技术 ⭐⭐ AZ显著优势

#### Zig
- **自研编译器**
- 直接生成机器码
- 增量编译
- 缓存系统
- 跨平台编译

**优点**:
- 编译速度快
- 完全控制
- 无外部依赖

**缺点**:
- 优化能力有限
- 需要自己实现所有优化
- 维护成本高

#### AZ
- **基于LLVM/MLIR**
- MLIR多级IR
- LLVM优化Pass
- LLD链接器
- LLDB调试器

**优点**:
- 强大的优化
- 成熟的生态
- 跨平台支持好
- 持续更新

**缺点**:
- 编译速度较慢
- 依赖LLVM
- 二进制体积大

**对比**:
- Zig编译更快
- AZ优化更好
- Zig更独立
- AZ生态更好

### 7. 标准库对比 ⚖️ 各有特色

#### Zig标准库
```zig
const std = @import("std");

// 文件I/O
const file = try std.fs.cwd().openFile("data.txt", .{});
defer file.close();

// 动态数组
var list = std.ArrayList(i32).init(allocator);
defer list.deinit();

// HashMap
var map = std.AutoHashMap([]const u8, i32).init(allocator);
defer map.deinit();

// 线程
const thread = try std.Thread.spawn(.{}, worker, .{});
thread.join();
```

#### AZ标准库
```az
import std.fs;
import std.collections;
import std.thread;

// 文件I/O
let file = std.fs.open("data.txt");
defer file.close();

// 动态数组
let list = std.collections.Vec<int>.new();

// HashMap
let map = std.collections.Map<string, int>.new();

// 线程
let handle = std.thread.spawn(|| {
    println("In thread");
});
handle.join();
```

**对比**:
- Zig需要显式allocator
- AZ使用默认allocator
- Zig更明确
- AZ更方便

### 8. 包管理 ✅ 都有完整方案

#### Zig
```zig
// build.zig.zon
.{
    .name = "myproject",
    .version = "0.1.0",
    .dependencies = .{
        .json = .{
            .url = "https://github.com/user/json/archive/v1.0.0.tar.gz",
            .hash = "1234...",
        },
    },
}
```

```bash
zig build
zig build run
zig build test
```

#### AZ
```az
// package.az
package {
    name: "myproject",
    version: "0.1.0",
    dependencies: {
        "json": "1.0.0",
        "http": "2.3.1"
    }
}
```

```bash
az_mod build
az_mod run
az_mod test
```

**对比**:
- 两者都有内置包管理
- Zig使用build.zig.zon
- AZ使用package.az
- 功能相似

### 9. 跨平台编译 ⭐ Zig优势

#### Zig
```bash
# 编译到不同平台
zig build -Dtarget=x86_64-linux
zig build -Dtarget=aarch64-macos
zig build -Dtarget=wasm32-freestanding

# 作为C编译器
zig cc main.c -o main
zig c++ main.cpp -o main
```

**特点**:
- 内置交叉编译
- 可以作为C/C++编译器
- 自带libc

#### AZ
```bash
# 编译到不同平台
az build --target x86_64-linux
az build --target aarch64-macos
az build --target wasm32

# 使用Clang编译C代码
clang main.c -o main
```

**特点**:
- 基于LLVM的交叉编译
- 依赖系统工具链
- 需要安装目标平台工具

**对比**:
- Zig的跨平台编译更方便
- Zig可以替代C编译器
- AZ依赖LLVM工具链

### 10. 性能对比 ⚖️ 理论上相近

#### Zig
- 接近C的性能
- 零成本抽象
- 编译时优化
- 手动内存管理

#### AZ
- 接近C的性能
- 零成本抽象
- LLVM优化
- 手动内存管理

**对比**:
- 运行时性能相近
- Zig编译更快
- AZ优化可能更好（LLVM）
- 都是系统级性能

### 11. 语法对比

#### 变量声明

**Zig**:
```zig
const x: i32 = 10;      // 不可变
var y: i32 = 20;        // 可变
const z = 30;           // 类型推导
```

**AZ**:
```az
let x: int = 10;        // 不可变
var y: int = 20;        // 可变
let z = 30;             // 类型推导
```

#### 函数定义

**Zig**:
```zig
fn add(a: i32, b: i32) i32 {
    return a + b;
}

pub fn public_func() void {
    // 公开函数
}
```

**AZ**:
```az
fn add(a: int, b: int) int {
    return a + b;
}

pub fn public_func() void {
    // 公开函数
}
```

#### 控制流

**Zig**:
```zig
// if表达式
const max = if (a > b) a else b;

// while循环
while (condition) {
    // ...
}

// for循环
for (items) |item| {
    // ...
}

// switch表达式
const result = switch (x) {
    0 => "zero",
    1 => "one",
    else => "other",
};
```

**AZ**:
```az
// if表达式（计划中）
let max = if (a > b) a else b;

// while循环
while (condition) {
    // ...
}

// for循环
for (item in items) {
    // ...
}

// match表达式
let result = match x {
    0 => "zero",
    1 => "one",
    _ => "other"
};
```

### 12. 特殊特性

#### Zig独有

1. **作为C编译器**
```bash
zig cc -o program main.c
```

2. **内联汇编**
```zig
asm volatile ("syscall"
    : [ret] "={rax}" (-> usize),
    : [number] "{rax}" (60),
      [arg1] "{rdi}" (0),
);
```

3. **向量类型**
```zig
const vec: @Vector(4, f32) = .{ 1, 2, 3, 4 };
```

4. **anytype**
```zig
fn print(value: anytype) void {
    // 编译时多态
}
```

#### AZ独有

1. **MLIR支持**
```az
// 可以生成和操作MLIR
#[mlir]
fn optimized_function() void {
    // MLIR优化
}
```

2. **可选GC**
```az
#[gc]
fn use_gc() void {
    let obj = new Object();
    // 自动回收
}
```

3. **Result类型**
```az
fn may_fail() Result<int> {
    // C3风格错误处理
}
```

4. **Match表达式**
```az
match value {
    pattern if guard => action,
    _ => default
}
```

## 生态系统对比

### Zig生态

**优势**:
- ✅ 活跃的社区
- ✅ 丰富的包
- ✅ 良好的文档
- ✅ 可以使用C库

**劣势**:
- ⚠️ 语言还在快速变化
- ⚠️ API不稳定
- ⚠️ 工具链较新

### AZ生态

**优势**:
- ✅ 基于LLVM生态
- ✅ 可以使用C/C++库
- ✅ MLIR支持
- ✅ 完整的工具链

**劣势**:
- ⚠️ 新语言，社区小
- ⚠️ 包较少
- ⚠️ 文档在建设中

## 学习曲线

### Zig
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
学习难度: ████████████░░░░░░░░ 60%
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

基础语法:     ████████░░░░░░░░░░░░ 40%
内存管理:     ████████████████░░░░ 80%
编译时执行:   ████████████████████ 100%
错误处理:     ████████░░░░░░░░░░░░ 40%
```

### AZ
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
学习难度: ██████████░░░░░░░░░░ 50%
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

基础语法:     ████████░░░░░░░░░░░░ 40%
内存管理:     ████████████░░░░░░░░ 60%
编译时执行:   ████████████░░░░░░░░ 60%
错误处理:     ████████████░░░░░░░░ 60%
```

## 使用场景

### Zig适合

1. ✅ 系统编程
2. ✅ 嵌入式开发
3. ✅ 游戏开发
4. ✅ 需要快速编译
5. ✅ 替代C/C++
6. ✅ 跨平台工具

### AZ适合

1. ✅ 系统编程
2. ✅ 编译器开发
3. ✅ 需要MLIR
4. ✅ 大型项目
5. ✅ 需要强大优化
6. ✅ 操作系统开发

## 总体评价

### Zig的优势

1. ✅ **成熟度高** - 已有大量用户
2. ✅ **编译快** - 自研编译器
3. ✅ **跨平台强** - 内置交叉编译
4. ✅ **简单直接** - 接近C的思维
5. ✅ **comptime强大** - 编译时元编程
6. ✅ **可替代C编译器** - 实用工具

### AZ的优势

1. ✅ **LLVM/MLIR** - 强大的技术栈
2. ✅ **优化能力强** - LLVM优化
3. ✅ **工具链完整** - 现代化工具
4. ✅ **模块系统** - 类似C3
5. ✅ **灵活的内存管理** - 多种选择
6. ✅ **Result类型** - 清晰的错误处理

## 相似度评分

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
AZ与Zig的相似度: ████████░░░░░░░░░░░░ 40%
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⚖️ 设计目标      ████████████░░░░░░░░ 60%
❌ 错误处理      ████░░░░░░░░░░░░░░░░ 20%
⚖️ 内存管理      ████████████░░░░░░░░ 60%
❌ 编译器技术    ░░░░░░░░░░░░░░░░░░░░  0%
✅ 编译时执行    ████████████████░░░░ 80%
⚖️ 语法          ████████████░░░░░░░░ 60%
⚖️ 包管理        ████████████████░░░░ 80%
```

## 选择建议

### 选择Zig，如果你：
- 需要快速编译
- 想要简单直接的语言
- 需要强大的跨平台编译
- 想替代C/C++编译器
- 喜欢comptime元编程
- 项目规模中小

### 选择AZ，如果你：
- 需要MLIR支持
- 想要强大的优化
- 需要完整的工具链
- 喜欢Result类型错误处理
- 项目规模大
- 需要灵活的内存管理选项

## 结论

**Zig和AZ都是优秀的现代系统编程语言，但采用了不同的技术路线：**

- **Zig** = 简单 + 快速 + 独立
- **AZ** = 强大 + 优化 + 生态

**Zig更像"更好的C"，AZ更像"现代系统语言"。**

两者都值得学习和使用，选择取决于你的具体需求！

---

**Zig: 简单而强大的系统编程语言** ⚡  
**AZ: 基于LLVM的现代系统编程语言** 🚀
